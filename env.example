# P1Lending ETL - Environment Configuration
# Copy this file to .env and fill in your values:
#   cp env.example .env
#
# NEVER commit .env to version control!

# ===========================================
# DATABASE
# ===========================================
POSTGRES_USER=p1lending
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=p1lending_etl

# ===========================================
# APPLICATION
# ===========================================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=generate-a-secure-random-key-here

# CORS Origins (JSON array format)
CORS_ORIGINS=["http://localhost:3000","http://your-ec2-ip","http://your-ec2-ip:80"]

# Frontend API URL (leave empty for relative URLs in production)
VITE_API_URL=

# ===========================================
# SNOWFLAKE
# ===========================================
SNOWFLAKE_ACCOUNT=HTWNYRU-CL36377
SNOWFLAKE_USER=SDABABNEH
SNOWFLAKE_ROLE=ACCOUNTADMIN
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=BULK_PROPERTY_DATA__NATIONAL_PRIVATE_SHARE
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_PRIVATE_KEY_PASSWORD=your_snowflake_key_password

# ===========================================
# GOOGLE SHEETS
# ===========================================
GOOGLE_SHEET_ID=19NHuPsTGVQQL95B-YO7hVSlb5jUgQBlP138JlTTuUFI

# Google Service Account Credentials
# Option 1 (Recommended): Store the entire JSON as a string in .env
# Use the helper script to convert your credentials file:
#   python backend/scripts/convert_google_credentials.py [path_to_google_credentials.json]
# Or manually: cat google_credentials.json | jq -c
# Then paste the output here (wrap in single quotes if needed)
GOOGLE_CREDENTIALS_JSON=

# Option 2: Use a file path (fallback if GOOGLE_CREDENTIALS_JSON is not set)
# If not set, the backend will automatically search for google_credentials.json in:
#   - /app/secrets/ (Docker production)
#   - backend/secrets/ (local development)
#   - backend/ (local development)
#   - Current working directory
# GOOGLE_CREDENTIALS_FILE=/app/secrets/google_credentials.json

# ===========================================
# CCC API (DNC Scrubbing)
# ===========================================
CCC_API_KEY=your_ccc_api_key_here
CCC_API_URL=https://dataapi.dncscrub.com/v1.4/scrub/litigator

# ===========================================
# idiCORE API
# ===========================================
IDICORE_CLIENT_ID=your_idicore_client_id
IDICORE_CLIENT_SECRET=your_idicore_client_secret

# ===========================================
# ETL SETTINGS
# ===========================================
ETL_BATCH_SIZE=200
LOG_LEVEL=INFO

